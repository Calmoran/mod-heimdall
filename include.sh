#!/usr/bin/env bash

# Sourced by AzerothCore's bash tooling (apps/bash_shared/common.sh) for every module.
# Heimdall's SQL lives in the standard data/sql layout and is applied by the core's own
# updater, so nothing needs declaring here. The file exists because the tooling looks for it.
